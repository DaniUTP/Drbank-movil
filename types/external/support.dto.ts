export interface SupportRequestDTO {
    last_name: string;
    email: string;
    reason: string;
    description: string;
    response: string;
}
export interface SupportResponseDTO {
    message: string;
}